#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(int argc, char *argv[])
{
    if (argc != 2)
    {
        prints(stderr, "Must have 2 arguments");
        exit(1);
    }
    prints(argv[1]);

    /*FILE *stream;
    stream = fopen(argv[1], "r");

    char character = '';
    
    
    while (character != "EOF")
    {
        character = fgetc(stream);
    }*/

}